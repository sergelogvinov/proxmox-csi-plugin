# ---
# apiVersion: snapshot.storage.k8s.io/v1
# kind: VolumeSnapshotClass
# metadata:
#   name: proxmox
# parameters:
#   storage: local
# driver: csi.proxmox.sinextra.dev
# deletionPolicy: Delete
---
apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: test
  namespace: default
spec:
  volumeSnapshotClassName: proxmox
  source:
    persistentVolumeClaimName: storage-test-0
